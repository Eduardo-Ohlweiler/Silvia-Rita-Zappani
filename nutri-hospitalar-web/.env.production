# Produção. Este arquivo VAI para o repositório: não há segredo aqui, só as
# URLs públicas — e elas precisam estar versionadas porque o Vite as resolve em
# tempo de BUILD e as embute no bundle. Trocar qualquer uma exige recompilar.

# Sem barra final: o refresh concatena `${baseURL}/auth/refresh` (services/api.ts).
VITE_API_URL=https://api.nutrisilviazappani.com.br

# Sem barra final: o index.html concatena `%VITE_SITE_URL%/og-image.png`.
VITE_SITE_URL=https://nutrisilviazappani.com.br
